// This file is generated by scripts/compile-typegpu-scenes.ts.
// Source: src/examples/svelte-motion/SvelteMotion.typegpu.svelte
// Do not edit this file directly.
import $renderer from 'svelte-typegpu/svelte-renderer';
import 'svelte/internal/disclose-version';
import * as $ from 'svelte/internal/client';
import * as TypeGpuAttributeValues from 'svelte-typegpu/internal/attribute-values';
import { onDestroy } from 'svelte';
import { Spring, Tween } from 'svelte/motion';
import { cubicInOut } from 'svelte/easing';
import MotionMarker from './MotionMarker.typegpu.js';
import { motionField } from './motion-field.js';

var root = $.from_tree([['mesh', null, ['boxGeometry'], ['standardMaterial']]], 4);

var root_1 = $.from_tree([
	[
		'scene',
		null,
		[
			'perspectiveCamera',
			null,
			[
				'controls',
				{ mode: 'orbit' },
				[
					'pointerControls',
					{ dragButton: 'primary', wheel: 'zoom', touch: 'orbit-pinch' }
				]
			]
		],
		' ',
		['ambientLight'],
		' ',
		['directionalLight'],
		' ',
		['group'],
		' ',,
	]
]);

export default function SvelteMotion_typegpu($$anchor, $$props) {
	var $$pop_renderer = $.push_renderer($renderer);

	$.push($$props, true);

	const position = new Tween([-7, 2, -7], { duration: 1600, easing: cubicInOut });
	const lift = new Spring(0, { stiffness: 0.06, damping: 0.45 });
	const appearance = new Tween(0, { duration: 900, easing: cubicInOut });

	function selectCell(event) {
		const position = event.target.attributes.position;

		$$props.onTargetChange(position[0], position[2]);
	}

	$.user_effect(() => {
		position.target = [$$props.controls.x, 2, $$props.controls.z];
	});

	$.user_effect(() => {
		lift.target = $$props.controls.lift;
	});

	$.user_effect(() => {
		appearance.target = $$props.controls.appearance;
	});

	onDestroy(() => {
		void position.set(position.current, { duration: 0 });
		void lift.set(lift.current, { instant: true });
		void appearance.set(appearance.current, { duration: 0 });
	});

	var scene = root_1();

	$.set_attribute(scene, 'clearColor', [0.035, 0.04, 0.04, 1]);

	var perspectiveCamera = $.child(scene);

	$.set_attribute(perspectiveCamera, 'active', true);
	$.set_attribute(perspectiveCamera, 'position', [22, 27, 33]);
	$.set_attribute(perspectiveCamera, 'target', [0, 0, 0]);
	$.set_attribute(perspectiveCamera, 'fov', 42);
	$.set_attribute(perspectiveCamera, 'near', 0.1);
	$.set_attribute(perspectiveCamera, 'far', 150);

	var controls_1 = $.child(perspectiveCamera);

	$.set_attribute(controls_1, 'minDistance', 20);
	$.set_attribute(controls_1, 'maxDistance', 85);
	$.reset(perspectiveCamera);

	var ambientLight = $.sibling(perspectiveCamera, 2);

	$.set_attribute(ambientLight, 'intensity', 0.4);
	$.set_attribute(ambientLight, 'color', [1, 1, 1]);

	var directionalLight = $.sibling(ambientLight, 2);

	$.set_attribute(directionalLight, 'position', [8, 20, 10]);
	$.set_attribute(directionalLight, 'lookAt', [0, 0, 0]);
	$.set_attribute(directionalLight, 'intensity', 0.9);

	var group = $.sibling(directionalLight, 2);

	$.each(group, 21, () => motionField.slice(0, $$props.controls.count), (cell) => cell.id, ($$anchor, cell) => {
		var mesh = root();
		var standardMaterial = $.sibling($.child(mesh));

		$.reset(mesh);

		$.template_effect(
			($0, $1, $2) => {
				$.set_attribute(mesh, 'position', $0);
				$.set_attribute(mesh, 'scale', $1);
				$.set_attribute(standardMaterial, 'color', $2);
			},
			[
				() => TypeGpuAttributeValues.value("position", $.get(cell).position),
				() => TypeGpuAttributeValues.value("scale", $.get(cell).scale),
				() => TypeGpuAttributeValues.value("color", $.get(cell).color)
			]
		);

		$.append($$anchor, mesh);
	});

	$.reset(group);

	var node = $.sibling(group, 2);

	MotionMarker(node, {
		get position() {
			return position.current;
		},

		get lift() {
			return lift.current;
		},

		get visible() {
			return $$props.controls.visible;
		},

		get appearance() {
			return appearance.current;
		}
	});

	$.reset(scene);
	$.event('click', group, selectCell);
	$.append($$anchor, scene);
	$.pop();
	$$pop_renderer();
}