// This file is generated by scripts/compile-typegpu-scenes.ts.
// Source: src/examples/occlusion/Sculpture.typegpu.svelte
// Do not edit this file directly.
import $renderer from 'svelte-typegpu/svelte-renderer';
import 'svelte/internal/disclose-version';
import * as $ from 'svelte/internal/client';
import * as TypeGpuAttributeValues from 'svelte-typegpu/internal/attribute-values';

var root = $.from_tree([['mesh', null, ['sphereGeometry'], ['standardMaterial']]], 4);

export default function Sculpture_typegpu($$anchor, $$props) {
	var $$pop_renderer = $.push_renderer($renderer);

	$.push($$props, true);

	let segments = $.prop($$props, 'segments', 3, 32);

	const position = $.derived(() => [
		($$props.index % 60 - 30) * 0.65,
		Math.floor($$props.index / 1200) * 0.58,
		-(Math.floor($$props.index / 60) % 20) * 1.3 - 2
	]);

	var mesh = root();
	var sphereGeometry = $.child(mesh);

	$.set_attribute(sphereGeometry, 'radius', 0.27);

	var standardMaterial = $.sibling(sphereGeometry);

	$.set_attribute(standardMaterial, 'color', [0.08, 0.66, 0.48]);
	$.set_attribute(standardMaterial, 'metalness', 0.15);
	$.set_attribute(standardMaterial, 'roughness', 0.5);
	$.reset(mesh);

	$.template_effect(
		($0) => {
			$.set_attribute(mesh, 'position', $0);
			$.set_attribute(sphereGeometry, 'widthSegments', segments());
			$.set_attribute(sphereGeometry, 'heightSegments', segments() / 2);
		},
		[
			() => TypeGpuAttributeValues.value("position", $.get(position))
		]
	);

	$.append($$anchor, mesh);
	$.pop();
	$$pop_renderer();
}