// This file is generated by scripts/compile-typegpu-scenes.ts.
// Source: src/examples/disco-shader-pass/DiscoShaderPass.typegpu.svelte
// Do not edit this file directly.
import $renderer from 'svelte-typegpu/svelte-renderer';
import 'svelte/internal/disclose-version';
import * as $ from 'svelte/internal/client';

import {
	discoFragment1,
	discoFragment2,
	discoFragment3,
	discoFragment4,
	discoFragment5,
	discoFragment6,
	discoFragment7
} from './disco-fragment.js';

var root = $.from_tree([
	[
		'scene',
		null,
		['perspectiveCamera', { id: 'main' }],
		' ',
		['shaderPass']
	]
]);

export default function DiscoShaderPass_typegpu($$anchor, $$props) {
	var $$pop_renderer = $.push_renderer($renderer);

	$.push($$props, true);

	const fragments = {
		pattern1: discoFragment1,
		pattern2: discoFragment2,
		pattern3: discoFragment3,
		pattern4: discoFragment4,
		pattern5: discoFragment5,
		pattern6: discoFragment6,
		pattern7: discoFragment7
	};

	const defaultDiscoControls = { pattern: 'pattern1' };
	let discoControls = $.prop($$props, 'controls', 3, defaultDiscoControls);
	var scene = root();

	$.set_attribute(scene, 'clearColor', [0, 0, 0, 1]);

	var perspectiveCamera = $.child(scene);

	$.set_attribute(perspectiveCamera, 'active', true);
	$.set_attribute(perspectiveCamera, 'position', [0, 0, 1]);
	$.set_attribute(perspectiveCamera, 'target', [0, 0, 0]);
	$.set_attribute(perspectiveCamera, 'fov', 45);
	$.set_attribute(perspectiveCamera, 'near', 0.1);
	$.set_attribute(perspectiveCamera, 'far', 10);

	var shaderPass = $.sibling(perspectiveCamera, 2);

	$.set_attribute(shaderPass, 'uniforms', { time: 'time', resolution: 'resolution' });
	$.set_attribute(shaderPass, 'renderOrder', 0);
	$.reset(scene);
	$.template_effect(() => $.set_attribute(shaderPass, 'fragment', fragments[discoControls().pattern]));
	$.append($$anchor, scene);
	$.pop();
	$$pop_renderer();
}