// This file is generated by scripts/compile-typegpu-scenes.ts.
// Source: src/examples/smoky-triangle/SmokyTriangle.typegpu.svelte
// Do not edit this file directly.
import $renderer from 'svelte-typegpu/svelte-renderer';
import 'svelte/internal/disclose-version';
import * as $ from 'svelte/internal/client';
import * as TypeGpuAttributeValues from 'svelte-typegpu/internal/attribute-values';
import { createSmokyTriangleFragment, defaultSmokyTriangleControls } from './smoky-triangle-fragment.js';

var root = $.from_tree([
	[
		'scene',
		null,
		['perspectiveCamera', { id: 'main' }],
		' ',
		['shaderPass']
	]
]);

export default function SmokyTriangle_typegpu($$anchor, $$props) {
	var $$pop_renderer = $.push_renderer($renderer);

	$.push($$props, true);

	let smokyTriangleControls = $.prop($$props, 'controls', 3, defaultSmokyTriangleControls);
	let smokyTriangleFragment = $.derived(() => createSmokyTriangleFragment(smokyTriangleControls()));

	let smokyTriangleUniforms = $.derived(() => ({
		time: 'time',
		resolution: 'resolution',
		value0: smokyTriangleControls().fromColor,
		value1: smokyTriangleControls().toColor
	}));

	var scene = root();

	$.set_attribute(scene, 'clearColor', [0.025, 0.03, 0.04, 1]);

	var perspectiveCamera = $.child(scene);

	$.set_attribute(perspectiveCamera, 'active', true);
	$.set_attribute(perspectiveCamera, 'position', [0, 0, 4]);
	$.set_attribute(perspectiveCamera, 'target', [0, 0, 0]);
	$.set_attribute(perspectiveCamera, 'fov', 45);
	$.set_attribute(perspectiveCamera, 'near', 0.1);
	$.set_attribute(perspectiveCamera, 'far', 20);

	var shaderPass = $.sibling(perspectiveCamera, 2);

	$.reset(scene);

	$.template_effect(
		($0) => {
			$.set_attribute(shaderPass, 'fragment', $.get(smokyTriangleFragment));
			$.set_attribute(shaderPass, 'uniforms', $0);
		},
		[
			() => TypeGpuAttributeValues.value("uniforms", $.get(smokyTriangleUniforms))
		]
	);

	$.append($$anchor, scene);
	$.pop();
	$$pop_renderer();
}