// This file is generated by scripts/compile-typegpu-scenes.ts.
// Source: src/examples/asset-world/Canoe.typegpu.svelte
// Do not edit this file directly.
import $renderer from 'svelte-typegpu/svelte-renderer';
import 'svelte/internal/disclose-version';
import * as $ from 'svelte/internal/client';
import { canoePosition } from './world.js';

var root = $.from_tree([['frameTask'], ' ', ['model', { name: 'canoe' }]], 1);

export default function Canoe_typegpu($$anchor, $$props) {
	var $$pop_renderer = $.push_renderer($renderer);

	$.push($$props, true);

	let paused = $.prop($$props, 'paused', 3, false),
		onselect = $.prop($$props, 'onselect', 3, () => {});

	let phase = $.state(0);

	function update({ delta }) {
		$.set(phase, ($.get(phase) + delta) % (Math.PI * 20));
	}

	var fragment = root();
	var frameTask = $.first_child(fragment);

	$.set_attribute(frameTask, 'update', update);

	var model = $.sibling(frameTask, 2);

	$.set_attribute(model, 'scale', 2.6);
	$.set_attribute(model, 'castShadow', true);
	$.set_attribute(model, 'receiveShadow', true);

	$.template_effect(
		($0, $1) => {
			$.set_attribute(frameTask, 'active', !paused());
			$.set_attribute(model, 'asset', $$props.asset);
			$.set_attribute(model, 'position', $0);
			$.set_attribute(model, 'rotation', $1);
		},
		[
			() => [
				canoePosition[0],
				canoePosition[1] + Math.sin($.get(phase) * 1.5) * 0.045,
				canoePosition[2]
			],

			() => [
				Math.sin($.get(phase)) * 0.025,
				-0.2,
				Math.sin($.get(phase) * 1.3) * 0.025
			]
		]
	);

	$.event('click', model, function (...$$args) {
		onselect()?.apply(this, $$args);
	});

	$.append($$anchor, fragment);
	$.pop();
	$$pop_renderer();
}