// This file is generated by scripts/compile-typegpu-scenes.ts.
// Source: src/examples/interactive-orbit-field/OrbitLights.typegpu.svelte
// Do not edit this file directly.
import $renderer from 'svelte-typegpu/svelte-renderer';
import 'svelte/internal/disclose-version';
import * as $ from 'svelte/internal/client';

var root = $.from_tree(
	[
		['ambientLight'],
		' ',
		['hemisphereLight'],
		' ',
		['directionalLight'],
		' ',
		['pointLight'],
		' ',
		['pointLight']
	],
	1
);

export default function OrbitLights_typegpu($$anchor) {
	var $$pop_renderer = $.push_renderer($renderer);
	var fragment = root();
	var ambientLight = $.first_child(fragment);

	$.set_attribute(ambientLight, 'color', [0.84, 0.94, 1]);
	$.set_attribute(ambientLight, 'intensity', 0.18);

	var hemisphereLight = $.sibling(ambientLight, 2);

	$.set_attribute(hemisphereLight, 'skyColor', [0.4, 0.62, 1]);
	$.set_attribute(hemisphereLight, 'groundColor', [0.12, 0.1, 0.08]);
	$.set_attribute(hemisphereLight, 'intensity', 0.34);

	var directionalLight = $.sibling(hemisphereLight, 2);

	$.set_attribute(directionalLight, 'rotation', [-0.84, 0.4, 0]);
	$.set_attribute(directionalLight, 'color', [1, 0.94, 0.82]);
	$.set_attribute(directionalLight, 'intensity', 1.35);

	var pointLight = $.sibling(directionalLight, 2);

	$.set_attribute(pointLight, 'position', [-2.3, 1.8, 2.2]);
	$.set_attribute(pointLight, 'color', [0.46, 0.92, 1]);
	$.set_attribute(pointLight, 'intensity', 4.4);
	$.set_attribute(pointLight, 'range', 9);

	var pointLight_1 = $.sibling(pointLight, 2);

	$.set_attribute(pointLight_1, 'position', [2.2, 1.1, 1.4]);
	$.set_attribute(pointLight_1, 'color', [1, 0.5, 0.26]);
	$.set_attribute(pointLight_1, 'intensity', 3.1);
	$.set_attribute(pointLight_1, 'range', 8);
	$.append($$anchor, fragment);
	$$pop_renderer();
}